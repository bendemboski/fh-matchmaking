{{#profile-page
  changeset=this.changeset
  backRoute="auth.host.bio"
  nextRoute="auth.host.review1"
as |page|
}}
  {{#page.left-column}}
    {{page.about-form changeset=this.changeset isHost=true}}
  {{/page.left-column}}
  {{#page.right-column}}
    {{#profile-page/testimonial-pic}}
      “Every bit that we can do about Homelessness, we are excited about! We are wanted to participate because it is the right thing to do.” -- Lex and Marjon, third BLOCK Hosts
    {{/profile-page/testimonial-pic}}
  {{/page.right-column}}

  {{page.footer percent=30}}
{{/profile-page}}
