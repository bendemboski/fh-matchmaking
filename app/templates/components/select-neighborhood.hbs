{{#power-select
  placeholder="Neighborhood"
  options=this.neighborhoodOptions
  selected=this.selectedNeighborhood
  searchField="label"
  disabled=@disabled
  triggerClass="neighborhood"
  renderInPlace=true
  onchange=(action this.onchange value="value")
as |neighborhood|
}}
  {{neighborhood.label}}
{{/power-select}}
