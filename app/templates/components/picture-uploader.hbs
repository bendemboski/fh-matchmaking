<img src={{@src}}
     alt={{@altText}}
     class='picture-uploader-preview'
     role='button'
     onclick={{action 'choosePicture'}}>
<input type='file' class='d-none' capture onchange={{action 'onPicture' value='target.files.0'}}>
