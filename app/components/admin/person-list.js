import Component from '@ember/component';

//
// This is a component for displaying host/resident lists in the admin pages.
// It wraps the table components in a header for switching between lists.
//
export default Component.extend();
